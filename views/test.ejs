<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>test</title>
  </head>
  <body>
    <table>
      <% for(var i=0; i < data.length; i++) { %>
      <tr>
         <td><b><%=i+1%>번의 자료: </b></td>
         <td><%= data[i].entry1 %></td>
         <td><%= data[i].entry2 %></td>
       </tr>
    <% } %>
    </table>

  </body>
</html>
